# Server Configuration
PORT=8888

# MongoDB Configuration
# For local development
MONGODB_URI=mongodb://localhost:27017/csm
# For production (replace with your MongoDB Atlas URI)
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/csm?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your_jwt_secret_here

# Environment
NODE_ENV=production
